[project]
name = "vizioner-api"
version = "0.1.0"
description = "Vizioner API service"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6.0",
    "fastapi>=0.127.0",
    "pydantic>=2.12.5",
    "redis>=7.1.0",
    "uvicorn>=0.40.0",
]

